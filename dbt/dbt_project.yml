name: nairobi_urban_intelligence
version: "1.0.0"
config-version: 2

profile: nairobi_urban_intelligence

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  nairobi_urban_intelligence:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
